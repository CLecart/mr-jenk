# ==============================================================================
# Environment Variables Template
#
# @description Environment variable template for the MR-Jenk project
# @warning     NEVER commit the real .env file!
# @usage       cp .env.example .env && nano .env
# ==============================================================================

# ------------------------------------------------------------------------------
# Jenkins Configuration
# ------------------------------------------------------------------------------

# Jenkins access port (default: 8080)
JENKINS_PORT=8080

# JNLP agent port (default: 50000)
JENKINS_AGENT_PORT=50000

# Secret for the distributed agent (generate after Jenkins setup)
# Obtain via: Jenkins > Manage Jenkins > Nodes > agent-1 > Show secret
JENKINS_AGENT_SECRET=

# ------------------------------------------------------------------------------
# Git/GitHub Configuration
# ------------------------------------------------------------------------------

# GitHub token (Personal Access Token with repo and admin:repo_hook rights)
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# E-commerce repository URL
GIT_REPO_URL=https://github.com/username/buy-01.git

# Main branch
GIT_BRANCH=main

# ------------------------------------------------------------------------------
# Notifications - Email (SMTP)
# ------------------------------------------------------------------------------

# SMTP server (examples: smtp.gmail.com, smtp.office365.com)
SMTP_HOST=smtp.gmail.com

# SMTP port (465 for SSL, 587 for TLS)
SMTP_PORT=587

# Sender email
SMTP_USER=jenkins-notifications@example.com

# Password / App Password
# For Gmail: https://myaccount.google.com/apppasswords
SMTP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Recipient list (comma separated)
NOTIFICATION_RECIPIENTS=team@example.com,lead@example.com

# ------------------------------------------------------------------------------
# Notifications - Slack (optional)
# ------------------------------------------------------------------------------

# Slack webhook URL
# Create at: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXXXX/YYYYY/ZZZZZ

# Slack channel (without #)
SLACK_CHANNEL=ci-notifications

# ------------------------------------------------------------------------------
# Deployment - Docker Registry
# ------------------------------------------------------------------------------

# Docker registry (Docker Hub, AWS ECR, etc.)
DOCKER_REGISTRY=docker.io

# Registry namespace/username
DOCKER_NAMESPACE=mycompany

# Docker credentials
DOCKER_USERNAME=
DOCKER_PASSWORD=

# ------------------------------------------------------------------------------
# Deployment - AWS (if used)
# ------------------------------------------------------------------------------

# AWS Access Key
AWS_ACCESS_KEY_ID=

# AWS Secret Key
AWS_SECRET_ACCESS_KEY=

# AWS region
AWS_REGION=eu-west-1

# ECS cluster (if deploying to ECS)
AWS_ECS_CLUSTER=

# ------------------------------------------------------------------------------
# Deployment - Heroku (if used)
# ------------------------------------------------------------------------------

# API Key Heroku
# Obtenir via: heroku auth:token
HEROKU_API_KEY=

# Nom de l'application Heroku
HEROKU_APP_NAME=

# ------------------------------------------------------------------------------
# Deployment - Local/SSH (if used)
# ------------------------------------------------------------------------------

# Deployment host
DEPLOY_HOST=192.168.1.100

# SSH user
DEPLOY_USER=deploy

# SSH port
DEPLOY_PORT=22

# Deployment path
DEPLOY_PATH=/opt/app

# ------------------------------------------------------------------------------
# Application Configuration
# ------------------------------------------------------------------------------

# Environment (dev, staging, prod)
ENVIRONMENT=dev

# Backend application port
APP_PORT=8081

# MongoDB URL
MONGODB_URI=mongodb://localhost:27017/buy01

# JWT Secret (generate with: openssl rand -hex 32)
# IMPORTANT: Use a different secret per environment
APP_JWT_SECRET=

# ==============================================================================
# IMPORTANT - Security
# ==============================================================================
#
# 1. NEVER commit this file with real values
# 2. Use Jenkins Credentials to store secrets
# 3. Generate unique tokens/passwords per environment
# 4. Revoke and rotate tokens if compromised
#
# ==============================================================================
